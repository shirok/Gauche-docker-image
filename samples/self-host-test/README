Perform self-host-test on development HEAD.

It pulls HEAD, compiles it on latest Gauche release, install it locally and
compiles it again with the HEAD version, then runs 'make -s check'.

Just invoke 'run' script in this directory to perform tests.


